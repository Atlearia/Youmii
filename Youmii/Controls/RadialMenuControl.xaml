<UserControl x:Class="Youmii.Controls.RadialMenuControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Youmii.Controls"
             xmlns:vm="clr-namespace:Youmii.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="250" d:DesignWidth="250">
    
    <UserControl.Resources>
        <!-- Fade In Animation for the entire menu -->
        <Storyboard x:Key="FadeInStoryboard">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                             From="0" To="1" Duration="0:0:0.25">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                             From="0.5" To="1" Duration="0:0:0.3">
                <DoubleAnimation.EasingFunction>
                    <BackEase EasingMode="EaseOut" Amplitude="0.3"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                             From="0.5" To="1" Duration="0:0:0.3">
                <DoubleAnimation.EasingFunction>
                    <BackEase EasingMode="EaseOut" Amplitude="0.3"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>
        
        <!-- Fade Out Animation -->
        <Storyboard x:Key="FadeOutStoryboard">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                             From="1" To="0" Duration="0:0:0.2">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseIn"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                             From="1" To="0.5" Duration="0:0:0.2">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseIn"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                             From="1" To="0.5" Duration="0:0:0.2">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseIn"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>

        <!-- Radial Menu Item Button Style -->
        <Style x:Key="RadialMenuItemStyle" TargetType="Button">
            <Setter Property="Width" Value="45"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <!-- Circular background -->
                            <Ellipse x:Name="Background"
                                     Fill="{StaticResource LightPinkBrush}"
                                     Stroke="{StaticResource SoftPinkBrush}"
                                     StrokeThickness="2">
                                <Ellipse.Effect>
                                    <DropShadowEffect Color="#FFE91E63" BlurRadius="8" ShadowDepth="2" Opacity="0.3"/>
                                </Ellipse.Effect>
                            </Ellipse>
                            
                            <!-- Icon -->
                            <TextBlock Text="{TemplateBinding Content}"
                                       FontSize="18"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Background" Property="Fill" Value="{StaticResource SoftPinkBrush}"/>
                                <Setter TargetName="Background" Property="Stroke" Value="{StaticResource PrimaryPinkBrush}"/>
                                <Setter TargetName="Background" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#FFE91E63" BlurRadius="12" ShadowDepth="3" Opacity="0.5"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Background" Property="Fill" Value="{StaticResource PrimaryPinkBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid x:Name="RootGrid" 
          Width="250" Height="250"
          RenderTransformOrigin="0.5,0.5"
          Opacity="0"
          IsHitTestVisible="False">
        <Grid.RenderTransform>
            <ScaleTransform ScaleX="0.5" ScaleY="0.5"/>
        </Grid.RenderTransform>
        
        <!-- Semi-transparent overlay circle behind menu items -->
        <Ellipse Width="200" Height="200"
                 HorizontalAlignment="Center"
                 VerticalAlignment="Center"
                 Fill="#40000000"
                 Opacity="0.3"/>
        
        <!-- Center close button -->
        <Button Width="40" Height="40"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Command="{Binding CloseMenuCommand}"
                Cursor="Hand">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Grid>
                        <Ellipse Fill="{StaticResource PrimaryPinkBrush}"
                                 Stroke="{StaticResource SoftPinkBrush}"
                                 StrokeThickness="2">
                            <Ellipse.Effect>
                                <DropShadowEffect Color="#FFE91E63" BlurRadius="10" ShadowDepth="2" Opacity="0.4"/>
                            </Ellipse.Effect>
                        </Ellipse>
                        <TextBlock Text="?" 
                                   Foreground="White"
                                   FontSize="16"
                                   FontWeight="Bold"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"/>
                    </Grid>
                </ControlTemplate>
            </Button.Template>
        </Button>

        <!-- Menu Items - Canvas centered in Grid, items positioned from center -->
        <ItemsControl ItemsSource="{Binding MenuItems}"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <!-- Canvas with origin at center (items use X/Y as offset from center) -->
                    <Canvas Width="0" Height="0"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemContainerStyle>
                <Style TargetType="ContentPresenter">
                    <Setter Property="Canvas.Left" Value="{Binding X}"/>
                    <Setter Property="Canvas.Top" Value="{Binding Y}"/>
                </Style>
            </ItemsControl.ItemContainerStyle>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Style="{StaticResource RadialMenuItemStyle}"
                            Content="{Binding Item.Icon}"
                            ToolTip="{Binding Item.Label}"
                            Command="{Binding DataContext.SelectItemCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                            CommandParameter="{Binding Item.Id}"
                            RenderTransformOrigin="0.5,0.5">
                        <Button.RenderTransform>
                            <!-- Offset by half the button size to center each button on its position -->
                            <TranslateTransform X="-22.5" Y="-22.5"/>
                        </Button.RenderTransform>
                    </Button>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
